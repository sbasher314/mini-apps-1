{
  "name": "challenge_3",
  "version": "1.0.0",
  "description": "Multistep checkout",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node server.js",
    "server-dev": "nodemon server.js",
    "build": "./node_modules/.bin/babel client --out-dir public --presets @babel/preset-react --source-maps inline",
    "build-dev": "./node_modules/.bin/babel client --out-dir public --presets @babel/preset-react --source-maps inline --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/cli": "^7.1.0",
    "@babel/core": "^7.13.15",
    "@babel/preset-env": "^7.1.0",
    "@babel/preset-react": "^7.0.0",
    "axios": "^0.21.1",
    "multer": "^1.4.2",
    "nodemon": "^2.0.7",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
